/**
 * The standard width of generated dungeon maps.
 */
export const MAP_WIDTH: number = 80;

/**
 * The standard height of generated dungeon maps.
 */
export const MAP_HEIGHT: number = 50;

/**
 * The minimum width of a dug room.
 */
export const MIN_ROOM_WIDTH: number = 4;

/**
 * The maximum width of a dug room.
 */
export const MAX_ROOM_WIDTH: number = 10;

/**
 * The minimum height of a dug room.
 */
export const MIN_ROOM_HEIGHT: number = 4;

/**
 * The maximum height of a dug room.
 */
export const MAX_ROOM_HEIGHT: number = 8;

/**
 * The minimum length of a dug corridor connecting rooms.
 */
export const MIN_CORRIDOR_LENGTH: number = 3;

/**
 * The maximum length of a dug corridor connecting rooms.
 */
export const MAX_CORRIDOR_LENGTH: number = 10;

/**
 * The percentage of tiles that must be dug before map generation stops.
 */
export const DUG_PERCENTAGE: number = 0.25;

/**
 * The maximum depth (number of floors) of the dungeon.
 */
export const MAX_DUNGEON_DEPTH: number = 5;

/**
 * The line-of-sight vision radius for FOV shadowcasting.
 */
export const FOV_RADIUS: number = 8;
