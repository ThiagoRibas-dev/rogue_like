/**
 * Identifiers for different types of UI tooltips.
 */
export const enum UITooltipType {
  Item = 'item',
  Status = 'status',
  Stat = 'stat',
  Entity = 'entity',
  Reaction = 'reaction',
  Tag = 'tag',
  Field = 'field'
}

/**
 * Identifiers for specific player stats displayed in the HUD.
 */
export const enum UIStatId {
  HP = 'hp',
  Attack = 'atk',
  Defense = 'def'
}
