/** LocalStorage key used to store the list of deterministically recorded replays. */
export const REPLAY_STORAGE_KEY = 'roguelike_replays';

/** Maximum number of replays to keep in the rolling local history. */
export const MAX_REPLAY_HISTORY = 5;
